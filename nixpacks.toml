[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["chmod +x mvnw && ./mvnw clean package -DskipTests"]

[start]
cmd = "java -Dserver.port=$PORT -jar target/ecommerce-backend-0.0.1-SNAPSHOT.jar"
